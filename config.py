# import yaml
import pathlib

PROJECT_ROOT = pathlib.Path(__file__).parent.resolve()

# def load_config(profile: str):
#     with open(PROJECT_ROOT / "profile" / f"{profile}.yml") as f:
#         return yaml.load(f, Loader=yaml.SafeLoader)
